# SalluLabs Watermark Remover v2 - Python Dependencies
# Install with: pip install -r requirements.txt

# Core (always required)
numpy>=1.21.0
opencv-python>=4.5.0
Pillow>=9.0.0

# PyTorch (required for LaMa AI model)
# ── CPU only:
#    pip install torch torchvision
#
# ── NVIDIA CUDA (fastest):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# ── AMD / Intel GPU on Windows (DirectML):
#    pip install torch torchvision torch-directml
#
# ── Apple Silicon (M1/M2/M3/M4 – MPS):
#    pip install torch torchvision   (MPS is built-in on macOS)
