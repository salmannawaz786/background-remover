# Copy this file to .env and update with your values

# Security - CHANGE THIS IN PRODUCTION!
SECRET_KEY=your-secret-key-here-change-this-in-production

# Performance Settings
# Workers: Set to number of physical cores (auto-detected if not set)
# For 2-core server: MAX_WORKERS=2
# Remove this line to auto-detect (recommended)
MAX_WORKERS=2

# Model: Now uses smart routing (RVM for persons, RMBG for objects)
# Options: fast, pro, or remove to auto-detect
MODEL_NAME=fast

# Firebase Configuration (Authentication only)
FIREBASE_CREDENTIALS_PATH=serviceAccountKey.json

# Cloudflare R2 Storage (for processed image uploads)
R2_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
R2_ACCESS_KEY=your-r2-access-key
R2_SECRET_KEY=your-r2-secret-key
R2_BUCKET_NAME=your-bucket-name
R2_PUBLIC_DOMAIN=

# Brevo Email Service (for OTP verification)
BREVO_API_KEY=xkeysib-YOUR-BREVO-API-KEY-HERE
BREVO_SENDER_EMAIL=noreply@sallulabs.com
